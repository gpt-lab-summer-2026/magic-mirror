TELEGRAM_BOT_TOKEN=""
GARMENT_BOT_PASSWORD=""

# The https URL cloudflared prints for the anchor page. The bot runs without it
# and simply never offers the page, so a dev machine can leave it empty.
ANCHOR_APP_URL=""
